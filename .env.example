# DuploCloud portal URL (required)
DUPLO_HOST=https://your-company.duplocloud.net

# API token from DuploCloud portal: User menu > Security > API Token (required)
DUPLO_TOKEN=your-api-token

# Default tenant name (optional — most tools accept tenant_id per-call)
DUPLO_TENANT=default
